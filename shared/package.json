{
  "name": "@portfolio/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Types, zod schemas and canonical content shared by the portfolio web app and API.",
  "exports": {
    ".": "./src/index.ts",
    "./content": "./src/content/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
